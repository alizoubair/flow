bedrock-agentcore>=1.4.0
strands-agents[otel]>=1.11.0
boto3>=1.40.1
requests>=2.31.0
# Observability: ADOT auto-instrumentation. Emits AgentCore memory/runtime
# spans + logs to CloudWatch when launched via `opentelemetry-instrument`.
aws-opentelemetry-distro>=0.10.0
